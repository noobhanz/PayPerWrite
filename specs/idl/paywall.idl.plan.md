# IDL Plan (v0.1)

Instructions:
- create_article(uri_encrypted, pay_mint, price, royalty_bps, transferable)
- set_article_price(article, price)
- purchase(article, referrer_opt)
- set_fee_config(protocol_fee_bps, referrer_fee_bps, treasury)

Accounts and events as defined in accounts.md and events.md.